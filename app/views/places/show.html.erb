<h1><%= @place["name"] %></h1>

  <% for post in @post %>
      <p>
      <h4><%= post["title"] %></h4>
      <h1><%= post["posted_on"] %></h1>
      <%= post["description"] %>
      </p>
  <% end %>

<p>
  <a href="/posts/new?place_id=<%= @place["id"] %>">New Post</a>
</p>
